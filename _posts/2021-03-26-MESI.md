---
layout: post
title: "🍎MESI协议"
date: 2021-03-26
tags:  Linux
---

MESI协议, 其实就是设计了M(modified)/E(exclusive)/S(shared)/I(invalid)四种状态, 以实现缓存一致性的协议. 首先, 我们需要了解MSI协议, 这是一个更简单更基础的缓存一致性协议.

## MSI协议

```
+--------+      +--------+
|  CPU0  |      |  CPUn  |
+---+----+      +---+----+
| S | BA |      | S | BA |
+---+----+ .... +---+----+
|        |      |        |
|        |      |        |
|        |      |        |
+--------+      +--------+
```

下文中的描述均是对于同一个Block的Cache Line来说的.

- M(Modified): 该Cache Line的值相较于内存中的值已经被修改了, 显然, 只允许一个CPU的缓存中存在M状态的Cache Line, 否则如果存在多个M, 最终内存中应更新为哪个值呢?
- S(Shared): 该Cache Line的值与内存中的值一致, 只是读取过, 显然多个CPU的缓存中都可能存在.
- I(Invalid): 相当于Cache Miss, 当前缓存中并无对应Block的值.

根据以上描述, 可以得到对于某个Block在多个CPU缓存中的Cache Line来说, 任意两个Cache Line状态冲突关系如下表所示:


