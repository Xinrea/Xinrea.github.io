---
layout: post
title: "☕️FinalFantasyXIV 内存数据的读取（2）"
date:   2017-09-20 20:27:00 +0800
tags: windows c++ memory
cover: '/assets/pic9202041.png'
---

上篇最后，给出了VS调试`ffxiv_dx11.exe`的图，可以看到，在我的环境下，其载入内存后基地址为`0x00007FF68AE90000`，若多次启动游戏进程，会发现其地址似乎是一个固定值，但是又似乎找不到这个地址从何而来，实际上，要弄清楚基址从何而来，需要了解程序运行的机制。

一个程序，有大量的信息储存在了PE头内，我们主要关心其中与基地址有关的信息，如下图所示：

![ffxiv_dx11.exe的PE头]({{ site.url }}/assets/pic9202041.png)

在程序运行时，获得的内存空间对于其本身来看是连续的，这其实是一块虚拟出来的内存，要对应到真实的物理地址还需要经过一系列操作才能达成，但对于进程本身视角来看，其操作管理的内存的确是连续的，因此对自己的空间不需要考虑过多的问题。

由于可以使用虚拟地址进行映射，程序运行的虚拟基地址并不会被其他程序所干扰，因此程序在运行时会被加载到同一个ImageBase地址上，如32位上的`0x00400000`，以及上图中所示的64位上的`0x0000000140000000`，在此基础上加上AdressOfEntryPoint即可得到入口地址。

这样看来，是不是首地址就相当于清晰地写在了文件里，只需要读出即可，而且固定不变呢？

问题当然没有这么简单，从上面的两张图中可以看出，实际与理论值完全不一样，这就涉及到了ASLR的问题。

>aslr是一种针对缓冲区溢出的安全保护技术，通过对堆、栈、共享库映射等线性区布局的随机化，通过增加攻击者预测目的地址的难度，防止攻击者直接定位攻击代码位置，达到阻止溢出攻击的目的的一种技术。

为了增强安全性，ASLR使加载时ImageBase并不是固定值，这一设定可以在编译器中进行设置，默认是开启的。启动加载时的ImageBase会随着某些状态的改变而发生变化，在固定状态下重复运行的话，基地址似乎也不会发生变化。

因此，要获取`ffxiv_dx11.exe`运行时的基地址，就只能动态地获取。

使用（1）中的方法便可动态获得游戏的基地址，接下来的任务便是定位变量。